import 'package:flutter/cupertino.dart';
import 'list_viewbuilder.dart';
import 'list_widget.dart';

class CollegeListBody extends StatelessWidget {
  const CollegeListBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ListViewBuilder();
  }
}
